import asyncio\nimport websockets\nimport json\nfrom typing import Dict, List, Optional, Callable\nfrom abc import ABC, abstractmethod\nimport aiohttp\nfrom datetime import datetime\nimport logging\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\n\nclass MarketDataProvider(ABC):\n    \"\"\"Abstract base class for market data providers\"\"\"\n    \n    @abstractmethod\n    async def connect(self):\n        \"\"\"Connect to the data source\"\"\"\n        pass\n    \n    @abstractmethod\n    async def disconnect(self):\n        \"\"\"Disconnect from the data source\"\"\"\n        pass\n    \n    @abstractmethod\n    async def subscribe(self, symbols: List[str], callback: Callable):\n        \"\"\"Subscribe to real-time data\"\"\"\n        pass\n    \n    @abstractmethod\n    async def get_historical_data(self, symbol: str, interval: str = '1m', limit: int = 100):\n        \"\"\"Fetch historical data\"\"\"\n        pass\n\n\nclass AlpacaDataProvider(MarketDataProvider):\n    \"\"\"Alpaca Markets real-time data provider\"\"\"\n    \n    def __init__(self, api_key: str, secret_key: str, base_url: str = \"https://data.alpaca.markets\"):\n        self.api_key = api_key\n        self.secret_key = secret_key\n        self.base_url = base_url\n        self.ws_url = \"wss://data.alpaca.markets/v1beta3/crypto/us/latest/trades\"\n        self.websocket = None\n        self.session = None\n        self.callbacks = {}\n    \n    async def connect(self):\n        \"\"\"Connect to Alpaca WebSocket\"\"\"\n        try:\n            headers = {\n                'Authorization': f'Bearer {self.api_key}',\n                'APCA-API-SECRET-KEY': self.secret_key\n            }\n            self.session = aiohttp.ClientSession(headers=headers)\n            logger.info(\"Alpaca connection initialized\")\n        except Exception as e:\n            logger.error(f\"Failed to connect to Alpaca: {e}\")\n            raise\n    \n    async def disconnect(self):\n        \"\"\"Disconnect from Alpaca\"\"\"\n        if self.websocket:\n            await self.websocket.close()\n        if self.session:\n            await self.session.close()\n        logger.info(\"Alpaca disconnected\")\n    \n    async def subscribe(self, symbols: List[str], callback: Callable):\n        \"\"\"Subscribe to real-time ticker data\"\"\"\n        try:\n            async with websockets.connect(self.ws_url) as websocket:\n                self.websocket = websocket\n                \n                # Subscribe to symbols\n                auth_message = {\n                    \"action\": \"auth\",\n                    \"key\": self.api_key,\n                    \"secret\": self.secret_key\n                }\n                await websocket.send(json.dumps(auth_message))\n                \n                subscribe_message = {\n                    \"action\": \"subscribe\",\n                    \"trades\": symbols,\n                    \"quotes\": symbols,\n                    \"bars\": symbols\n                }\n                await websocket.send(json.dumps(subscribe_message))\n                \n                # Listen for messages\n                async for message in websocket:\n                    data = json.loads(message)\n                    await callback(data)\n        except Exception as e:\n            logger.error(f\"Subscription error: {e}\")\n            raise\n    \n    async def get_historical_data(self, symbol: str, interval: str = '1m', limit: int = 100):\n        \"\"\"Fetch historical bars from Alpaca\"\"\"\n        try:\n            url = f\"{self.base_url}/v1beta3/crypto/us/bars\"\n            params = {\n                'symbols': symbol,\n                'timeframe': interval,\n                'limit': limit\n            }\n            \n            async with self.session.get(url, params=params) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    logger.info(f\"Retrieved {len(data)} bars for {symbol}\")\n                    return data\n                else:\n                    logger.error(f\"API error: {response.status}\")\n                    return None\n        except Exception as e:\n            logger.error(f\"Failed to get historical data: {e}\")\n            return None\n\n\nclass DXFeedDataProvider(MarketDataProvider):\n    \"\"\"dxFeed market data provider\"\"\"\n    \n    def __init__(self, api_key: str, base_url: str = \"https://demo.dxfeed.com\"):\n        self.api_key = api_key\n        self.base_url = base_url\n        self.ws_url = \"wss://demo.dxfeed.com/webapi/v2\"\n        self.websocket = None\n        self.session = None\n    \n    async def connect(self):\n        \"\"\"Connect to dxFeed WebSocket\"\"\"\n        try:\n            self.session = aiohttp.ClientSession()\n            logger.info(\"dxFeed connection initialized\")\n        except Exception as e:\n            logger.error(f\"Failed to connect to dxFeed: {e}\")\n            raise\n    \n    async def disconnect(self):\n        \"\"\"Disconnect from dxFeed\"\"\"\n        if self.websocket:\n            await self.websocket.close()\n        if self.session:\n            await self.session.close()\n        logger.info(\"dxFeed disconnected\")\n    \n    async def subscribe(self, symbols: List[str], callback: Callable):\n        \"\"\"Subscribe to real-time data\"\"\"\n        try:\n            async with websockets.connect(self.ws_url) as websocket:\n                self.websocket = websocket\n                \n                # Subscribe to symbols\n                subscribe_msg = {\n                    \"auth\": {\"token\": self.api_key},\n                    \"setup\": {\"acceptAggregationPeriod\": 100},\n                    \"subscription\": {\n                        \"symbols\": symbols,\n                        \"types\": [\"Trade\", \"Quote\", \"Summary\"]\n                    }\n                }\n                await websocket.send(json.dumps(subscribe_msg))\n                \n                # Listen for messages\n                async for message in websocket:\n                    data = json.loads(message)\n                    await callback(data)\n        except Exception as e:\n            logger.error(f\"Subscription error: {e}\")\n            raise\n    \n    async def get_historical_data(self, symbol: str, interval: str = '1m', limit: int = 100):\n        \"\"\"Fetch historical data from dxFeed\"\"\"\n        try:\n            url = f\"{self.base_url}/rest/data.json\"\n            params = {\n                'symbols': symbol,\n                'types': 'candles',\n                'interval': interval,\n                'count': limit\n            }\n            headers = {'Authorization': f'Bearer {self.api_key}'}\n            \n            async with self.session.get(url, params=params, headers=headers) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    logger.info(f\"Retrieved {len(data)} bars for {symbol}\")\n                    return data\n                else:\n                    logger.error(f\"API error: {response.status}\")\n                    return None\n        except Exception as e:\n            logger.error(f\"Failed to get historical data: {e}\")\n            return None\n\n\nclass AlphaVantageDataProvider(MarketDataProvider):\n    \"\"\"Alpha Vantage market data provider (REST-based)\"\"\"\n    \n    def __init__(self, api_key: str, base_url: str = \"https://www.alphavantage.co/query\"):\n        self.api_key = api_key\n        self.base_url = base_url\n        self.session = None\n        self.data_cache = {}\n    \n    async def connect(self):\n        \"\"\"Initialize connection\"\"\"\n        try:\n            self.session = aiohttp.ClientSession()\n            logger.info(\"Alpha Vantage connection initialized\")\n        except Exception as e:\n            logger.error(f\"Failed to connect to Alpha Vantage: {e}\")\n            raise\n    \n    async def disconnect(self):\n        \"\"\"Close connection\"\"\"\n        if self.session:\n            await self.session.close()\n        logger.info(\"Alpha Vantage disconnected\")\n    \n    async def subscribe(self, symbols: List[str], callback: Callable):\n        \"\"\"Poll for real-time data (Alpha Vantage doesn't support WebSocket)\"\"\"\n        try:\n            while True:\n                for symbol in symbols:\n                    data = await self.get_quote(symbol)\n                    if data:\n                        await callback(data)\n                await asyncio.sleep(1)  # Rate limiting\n        except Exception as e:\n            logger.error(f\"Subscription error: {e}\")\n            raise\n    \n    async def get_quote(self, symbol: str):\n        \"\"\"Get real-time quote\"\"\"\n        try:\n            params = {\n                'function': 'GLOBAL_QUOTE',\n                'symbol': symbol,\n                'apikey': self.api_key\n            }\n            \n            async with self.session.get(self.base_url, params=params) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    return {\n                        'symbol': symbol,\n                        'timestamp': datetime.now().isoformat(),\n                        'quote': data.get('Global Quote', {})\n                    }\n                return None\n        except Exception as e:\n            logger.error(f\"Failed to get quote for {symbol}: {e}\")\n            return None\n    \n    async def get_historical_data(self, symbol: str, interval: str = '1min', limit: int = 100):\n        \"\"\"Fetch historical intraday data\"\"\"\n        try:\n            params = {\n                'function': f'TIME_SERIES_INTRADAY',\n                'symbol': symbol,\n                'interval': interval,\n                'apikey': self.api_key\n            }\n            \n            async with self.session.get(self.base_url, params=params) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    # Extract time series\n                    key = f'Time Series ({interval})'\n                    if key in data:\n                        series = data[key]\n                        items = list(series.items())[:limit]\n                        logger.info(f\"Retrieved {len(items)} bars for {symbol}\")\n                        return items\n                return None\n        except Exception as e:\n            logger.error(f\"Failed to get historical data: {e}\")\n            return None\n\n\nclass CMEGroupDataProvider(MarketDataProvider):\n    \"\"\"CME Group derivatives data provider\"\"\"\n    \n    def __init__(self, api_key: str, base_url: str = \"https://www.cmegroup.com/trading/market_data\"):\n        self.api_key = api_key\n        self.base_url = base_url\n        self.session = None\n    \n    async def connect(self):\n        \"\"\"Initialize connection\"\"\"\n        try:\n            headers = {'Authorization': f'Bearer {self.api_key}'}\n            self.session = aiohttp.ClientSession(headers=headers)\n            logger.info(\"CME Group connection initialized\")\n        except Exception as e:\n            logger.error(f\"Failed to connect to CME Group: {e}\")\n            raise\n    \n    async def disconnect(self):\n        \"\"\"Close connection\"\"\"\n        if self.session:\n            await self.session.close()\n        logger.info(\"CME Group disconnected\")\n    \n    async def subscribe(self, symbols: List[str], callback: Callable):\n        \"\"\"Poll for real-time futures data\"\"\"\n        try:\n            while True:\n                for symbol in symbols:\n                    data = await self.get_futures_quote(symbol)\n                    if data:\n                        await callback(data)\n                await asyncio.sleep(1)\n        except Exception as e:\n            logger.error(f\"Subscription error: {e}\")\n            raise\n    \n    async def get_futures_quote(self, symbol: str):\n        \"\"\"Get real-time futures quote\"\"\"\n        try:\n            url = f\"{self.base_url}/quotes/{symbol}\"\n            async with self.session.get(url) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    return {\n                        'symbol': symbol,\n                        'timestamp': datetime.now().isoformat(),\n                        'data': data\n                    }\n                return None\n        except Exception as e:\n            logger.error(f\"Failed to get futures quote: {e}\")\n            return None\n    \n    async def get_historical_data(self, symbol: str, interval: str = '1m', limit: int = 100):\n        \"\"\"Fetch historical futures data\"\"\"\n        try:\n            url = f\"{self.base_url}/historical/{symbol}\"\n            params = {'interval': interval, 'limit': limit}\n            \n            async with self.session.get(url, params=params) as response:\n                if response.status == 200:\n                    data = await response.json()\n                    logger.info(f\"Retrieved {len(data)} bars for {symbol}\")\n                    return data\n                return None\n        except Exception as e:\n            logger.error(f\"Failed to get historical data: {e}\")\n            return None\n"